# Copy to mcp_agent.secrets.yaml and fill in your API keys.
# Do NOT commit the actual secrets file.

anthropic:
  api_key: "${ANTHROPIC_API_KEY}"

# Optional: for Google Gemini provider
# google:
#   api_key: "${GOOGLE_API_KEY}"
